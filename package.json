{
  "name": "serverless-crawler",
  "version": "1.0.0",
  "description": "Distributed serverless web crawling/web scraping with support for js execution, proxying, and autoscaling",
  "main": "lib/fn.js",
  "bin": {
    "cli": "bin/cli.js"
  },
  "directories": {
    "example": "example",
    "lib": "lib"
  },
  "scripts": {
    "test": "mocha --exit --grep @io --invert --timeout 10000 './{,!(node_modules)/**/}*.spec.js'",
    "test:watch": "mocha --grep @io --invert --timeout 10000 --watch './{,!(node_modules)/**/}*.spec.js'"
  },
  "author": "Ani Channarasappa",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^5.2.0",
    "prettyjson": "^1.2.1",
    "shelljs": "^0.8.3",
    "sinon": "^7.2.3"
  },
  "dependencies": {
    "inquirer": "^6.2.1",
    "ioredis": "^4.3.0",
    "puppeteer": "^1.11.0",
    "ramda": "^0.26.1",
    "yargs": "^12.0.5"
  }
}
